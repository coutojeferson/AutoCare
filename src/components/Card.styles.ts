import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  card: {
    padding: 16,
    borderWidth: 1,
    borderColor: '#DDD',
    borderRadius: 12,
    backgroundColor: '#FFF',
  },
  vehicleInfo: {
    fontSize: 14,
    color: '#555',
  },
  vehicleName: {
    fontSize: 18,
    fontWeight: '700',
    marginBottom: 6,
  },
});
